# Independent scene verifier — GLM Flash

Objective: Independently verify a forthcoming DeepSeek-produced controller relocation from immutable v9 into v10. Build the verification script now while scene and render workers run in parallel. This is a fresh-context review: do not read producer transcripts or specs. Parent owns high-level review. You are not alone; preserve other workers' edits.

Files: Work under C:/Projects/CAD/astra-engine-pump only. Read applicable AGENTS.md, ASTRA-HANDOFF-2026-09-18.md, tools/blender_move_controller.py, and reports/astra-baseline-v9.json if it exists. Own tools/astra_verify_v10.py and reports/astra-verifier-preparation.md only. Do not change scene, render scripts, source CAD, existing reports or other repositories. Proposed target tools/astra_controller_v10.py may be read when available for code review but verification must derive its own scene observations.

Interfaces: Blender Python verifier accepting absolute --baseline and --candidate blend paths, and --report JSON output. In one Blender process load v9 then v10 sequentially, snapshot data sufficient to compare all mesh geometry/material assignment/parenting/world transforms, collection membership and object names; allow only the intended controller assembly rigid translation and deletion of CAD_QUARANTINE_outliers duplicate. Confirm which objects belong to controller via source scene and explicit bracket PP-CPM-1005. Check bracket bottom 1.2192 metres above skid bottom, mate plane separation and normal alignment against engine-side minus-X upright LBU-PP.001 where determinable. Do not require baseline materials unchanged if they're purely metadata outside scene worker scope? For this scene change, yes all materials should be unchanged; rendering changes happen only later in memory. Capture total source triangles and moved names, unchanged pump shaft placement and shape. Use chunked bulk vertex access for memory, cryptographic digests of coordinates/topology. Exit nonzero on real violations. Do not save a blend. Quarantine removal is permitted only if it existed. Baseline report is guidance not proof; reload original scene at actual run.

Constraints: No Blender launch in this preparation phase (scene worker holds the memory-heavy slot). Static compile only. No cloud, package installs, global config changes, source overwrite, commits or publication. Native Write/Edit for files. Keep script focused and robust to Blender 5.1. Do not invoke any subagents. Skills/plugins disabled by bare mode; exact spec is sufficient. No unrelated queue docs or broad repo scans.

Verification: compile script with installed Python; report exact invocation with C:/Program Files/Blender Foundation/Blender 5.1/blender.exe --background --threads 4 --python ... -- absolute args. Explain any constraints that cannot yet be asserted and how to test them. Output concise preparation report, then finish; parent will authorize actual run after v10 exists. Do not claim verification passed without execution.

REASONING: high. Implement now.
