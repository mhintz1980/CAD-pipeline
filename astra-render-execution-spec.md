# Cross-family render review and execution — DeepSeek Flash

Objective: Review GLM's prepared preview script, fix concrete defects, then render the v10 engine/pump package under Mark's latest Pioneer livery direction. Preserve source scenes. Parent will dispatch this only after GLM preparation finishes and independent verifier releases Blender slot. You own render execution; other workers remain in this folder, do not revert their work.

Files: Read tools/astra_render_preview.py, reports/astra-render-plan.md, ASTRA-LIVERY-DIRECTION-2026-09-18.md and reports/astra-independent-verification.json in C:/Projects/CAD/astra-engine-pump. Read owner handoff as needed; no broad repo/docs reread. Now own tools/astra_render_preview.py for fixes (GLM producer has finished), reports/astra-preview/ output subtree, reports/astra-render-execution.md, reports/astra-preview-materials.json. Source v9/v10 immutable. No pipeline repo edits. Use existing calibrated material behavior with new palette; no MSD yellow assumption.

Interfaces: Review script for Blender 5.1 compatibility, no CAD mesh/world matrix mutations, colour role mapping, correct sRGB-to-linear shader handling, bounding-box camera framing and genuine contact shadows. Explicitly paint engine castings John Deere green while preserving metal/rubber/exhaust where separable. Working digital colors are approximations, label them. PMS293 first preview unless latest file says Process Blue. User's five-digit grey is invalid and must not be guessed. Run on out/engine-and-pump-v10.blend. Render overall three-quarter with entire machine visible plus legible controller-detail image; camera should reveal minus-X controller. Use moderate resolution/samples, 4 threads, one process sequential frames, avoid repeated geometry imports. Save a new materialized preview .blend inside reports/astra-preview/ for owner inspection, no source overwrite. Exclude render helpers from framing/geometry invariance. Compute hashes of source before and after; prove no CAD transform or mesh changes during look-dev. Parent views PNGs for high-level visual review.

Constraints: One Blender process only after confirming other task Blender jobs ended. Do not kill unrelated jobs. No cloud, packages, global configuration, publishing, commits, adding missing engineering geometry or touching pump placement. Native Write/Edit for script/report edits. Fix concrete issues; do not redesign. If Cycles crashes after saving valid outputs, report exit honestly and assess saved artifacts, do not call that a clean process pass. Do not declare owner visual acceptance. Do not add text overlays or invented branding logos.

Verification: actual run with captured exit/log; images load successfully and have nontrivial pixel variation, output .blend exists, source hashes unchanged, report achieved checks and unresolved limitations. If generation fails, repair bounded defects and rerun. Final concise report includes exact local artifact paths, script changes, timings, exit status and cross-family review verdict. Also correct reports/astra-code-review.md section 5 stale claim that render worker never ran: initial authentication failure was recovered by retry and later GLM script production. You may update that report section only, preserving historical limitation wording as appropriate.

REASONING: high. Execute through to reviewable artifacts, not just a plan.
