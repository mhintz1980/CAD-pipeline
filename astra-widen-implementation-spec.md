# Widen v10 into v11 — DeepSeek execution worker

User CONFIRMED 6 inches total, +3 inches per side. Astra owns the following strategy; implement it with measured guards, do not redesign or globally scale. Root C:/Projects/CAD/astra-engine-pump. Other workers own inspection script/reports and wide-render script; preserve them. User-owned Blender GUI PID13752 has v10 open: never touch, close or wait for it. Only coordinate BACKGROUND Blender jobs.

Source immutable: out/engine-and-pump-v10.blend. Read ASTRA-WIDENING-PLAN-2026-09-18.md, reports/astra-widening-inspection.json, and inspection markdown if ready. Own tools/astra_widen_v11.py, out/engine-and-pump-v11-wide.blend, reports/astra-widening-v11.json, reports/astra-widening-v11.md/log, and uniquely named diagnostics. Use native Write/Edit. No cloud/install/global-config/commits. Never overwrite source, v9 or existing render scenes.

PART POLICY (derive exact lists from 468-mesh source and assert them):
- KEEP FIXED: all 415 CAD_pump_new meshes, all 7 CAD_engine meshes, DOUBLER and DOUBLER.001, MNT-PEB-CTF-PP12-WIDE, PLT-MNT-6SL-PE-PP12, all four PP128S22-MFB* end mid-brackets. These preserve central mounts and engine/pump placement.
- RIGID OUTWARD +/-76.2 mm by side: the six longitudinal MC channel skid members; PP-FTS-1005 and .001 side rails; four PP-FBS* outside braces; all eight CAD_bale parts except LBA-CB (uprights, LBS shelves/gussets, PP-LBB plates). Determine sign from measured side of X=59 mm. Preserve local mesh hashes. These should be 20 mesh objects.
- Controller: translate CPM-PP-JD18 root by -76.2 mm X once (3 mesh children), preserving its relative placement to LBU-PP.001, its height and normal alignment.
- LENGTHEN 15 X-spanning parts: PP-FTT and .001 deck plates; six PP-FTS-1008* crossmembers; two Copy of Copy of Part11^PP128-FTA* transverse end pieces; two Part1^PP128-PP108-SKID* end handles; two tube rectangular_ai_TR12x6x0.25x80* transverse tubes; LBA-CB top bale beam.

LENGTHENING STRATEGY: preserve core coordinates, insert length into straight feature-free spans on both sides. Candidate world-X cut planes are -680 mm and +798 mm (symmetric about X=59). In the inspected data these lie within safe bands for PP-FTT (-763.73..-605.17,722.83..882.19), PP-FTS-1008 (-789.45..-474.75,593.25..907.91), LBA-CB (-842.19..-205.26,303.74..941.12). Check ALL 15 parts against actual geometry and their feature_free_bands. For each lengthened mesh, vertices left of -680 shift -76.2mm, vertices right of +798 shift +76.2mm, vertices in central band stay fixed. Long planar/straight-extrusion triangles bridging cut planes thereby lengthen without rescaling section or bore shape. This is NOT acceptable if a cut crosses a curved feature, hole, end-cap or non-X-parallel surface; assert safety from world-space face normals and curved transverse features. If either plane is unsafe for a part, find another FEATURE-FREE plane between fixed core mount features and end structure, report numeric choice and reason; never stretch a hole or silently move central mounts. Baseline curved transverse feature edge lengths must be preserved; X-directed extrusion edges may lengthen. Preserve Y/Z for all modified vertices. Make mesh single-user before editing if shared; avoid repeated deformation of duplicates. Topology/connected components/manifold defect counts must remain as baseline. Use world-to-local transforms correctly (some source origins are very large with mesh offsets).

Expected 23 rigidly translated meshes, 15 lengthened meshes, 430 meshes fixed; total 468. Validate lists first; if actual source changed, report rather than forcing counts. Full skid outside width increases exactly152.4mm; both original asymmetrical aggregate bbox centre and nominal sidewall centre59mm stay unchanged. Upright inner spacing increases152.4mm. Y/Z scene extents unchanged. The central mounting plates/end mid-brackets stay fixed and sit on still-continuous extended crossmembers. Preserve existing mating/overlap relationships for sidewall/bale base plates and shelves; validate positions relative to moved uprights and extended deck regions.

Verification in ONE memory-aware Blender job, 4 threads: hash source before/after; assertions on exact mesh sets, unchanged pump/engine geometry/matrices, fixed central mounting vertices/hole geometry, rigid-part local hashes, preserved Y/Z and topology on lengthened parts, <=0.1mm tolerances where float32 requires. Save NEW v11 only after structural guards pass. Reload v11 and recheck main invariants. Measure engine/upright clearance AFTER with actual evaluated mesh/BVH queries (reuse inspection logic read-only where safe with new report paths). Do not simply add76.2 to previous sampled distance. Report intersections and sampled separation bounds honestly, plus baseline comparisons13.559mm left/22.959mm right. Fail process via --python-exit-code1 or explicit nonzero exit on failed checks.

No rendering in this task; separate worker handles it after independent verification. Output concise report with exact part operation table/cut planes, widths before-after, clearances, source and output hashes, and remaining limitations. This is mesh geometry revision in Blender, not STEP rebuild; do not claim owner visual acceptance. REASONING high. Execute now through a saved verified candidate, not another exploratory study.
