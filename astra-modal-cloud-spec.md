# Urgent Modal execution worker
REASONING: low. You are the infrastructure implementer. User explicitly authorized Modal GPUs/RAM to finish engine export within one hour (deadline 2026-09-18 14:44 EDT). Work immediately, no broad exploration. Another worker owns engine processing.

Own tools/astra_modal_engine.py and reports/astra-modal-engine/** only. Create and RUN a bounded Modal Function, not a deployed idle service. Use existing authenticated Modal Python314 CLI. Reuse Blender image patterns from C:/Projects/msp-render-pipeline (read-only) if useful. Blender 5.1 compatible with input. Modal resources 16 CPU, 65536 MiB RAM, GPU L4, timeout 2400 seconds, max_containers=1, no retries. Request/limit tuples if supported to bound CPU/RAM. Budget estimate about $1.55 per45min compute plus build/transfer already communicated by architect; no need ask. Do not log secrets.

Source immutable C:/Projects/CAD/astra-engine-pump/out/engine-and-pump-v10.blend (102843862 bytes, SHA256 5f8632d8ef9b27da15b221f47feb9fb37dcea7b52493aeba62125523ae5f5e6f). Transfer privately to Modal. Never upload CAD to public GitHub. Repo origin CAD-pipeline; no pull/commit/push in this seat. Set up cloud image/upload NOW while sibling writes tools/astra_engine_export.py. Interface cloud command: blender -b /input/engine-and-pump-v10.blend --python /scripts/astra_engine_export.py -- --output-dir /output. Script expected within minutes. Function may use add_local_file or remote bytes for input/script; persist output via Modal Volume unique prefix and download all outputs into reports/astra-modal-engine. Preserve local originals. Ensure job log, Modal app/function ids and receipt JSON saved. On errors fix your wrapper and retry within deadline. Do not run Blender locally. Gather GPU nvidia-smi and system memory evidence. Need outputs back promptly, not just cloud completion. Write compact status file early while image builds. Read current Modal docs as necessary, do not waste time on huge guides. Verify syntax and actual Modal execution. Monitor until downloaded. Source SHA unchanged. Logs should not be truncated on disk. Return measured results, output paths, limitations.
