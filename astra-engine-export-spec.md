# Urgent cloud Blender engine export implementation
REASONING: low. Deadline 2026-09-18 14:44 EDT; begin coding now. Own tools/astra_engine_export.py only and reports/astra-engine-export-code.md. Another worker provides Modal Blender5.1,16CPU64GiB L4 and runs your script. Do NOT launch local Blender or cloud yourself. Write runnable script within 5min, then refine static issues quickly.

Interface blender -b /input/engine-and-pump-v10.blend --python /scripts/astra_engine_export.py -- --output-dir /output. Use argparse after --. Select all mesh objects recursively in CAD_engine collection (7 meshes; ~2.277M tris), nothing from pump/skid/bale. Preserve world geometry, world units (scene unit_scale verify, source meters expected); export explicit millimetres and same assembly origin/orientation. Input scene X[-835.717,837.721]Y[-1762.413,521.779]Z[-482.756,1715.81]mm roughly engine bounds; verify not hardcode. Save engine-source-extracted.blend optional avoid huge output; required engine-cleaned.blend and binary STL light ~200k triangles(10MB), detailed ~500k(25MB), README import instructions and JSON report. Preserve individual bodies in blend. Process mesh copies with applied world transforms, no source mutation. Conservative cleanup remove duplicates/degenerate edges/faces tiny distance <=0.001mm, recalc consistent normals; do NOT blindly fill intentional bores/open ports, no voxel fill of assembly clearances. Record boundary/nonmanifold/degenerate stats before and after. Decimate each body proportional to triangle count, protect small mounting interfaces (objects other than COMPOUND / COMPOUND.001) at full resolution. Bbox and sample bidirectional surface deviations evaluated against original (BVH samples deterministic, report estimates not certified Hausdorff). Prefer loss ratio that achieves targets, not uncompensated overshoot. Warn mesh may retain intentional boundaries. Validate STL header/count/filesize and nonempty finite geometry; actual measured counts and dimensions. Output is SW graphics/mesh reference, NOT analytic solid nor watertight promise.

Use Blender bpy/bmesh/mathutils only plus standard Python/numpy bundled. Efficient foreach_get numpy when possible; avoid multi-million python object heap duplication. Decimation largely CPU even though L4 available for Cycles validation render. Produce engine-preview.png 1000x800 neutral background, JDgreen #367C2B castings, blackrubber/metal retained if practical; ensure GPU Cycles OPTIX or CUDA chosen with evidence. Frame actual engine bounds; discard nonengine objects from output scene. Render <=64samples modest time; CPU fallback if GPU unavailable report honestly. Save .blend AFTER engine isolated and BEFORE light export final decimations if useful. Validate preservation of mounts and measure changes. Do not alter upstream original files. Run py_compile only locally, hand execution to cloud worker. Publish script quickly, write report with dependencies/interface. No additional planning document reads necessary.
