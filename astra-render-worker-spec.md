# Render preparation worker — GLM-5.3-Flash

Objective: Prepare a reproducible local first-look rendering of the accepted engine/pump v9 plus controller relocation v10. Implement preparation while a separate DeepSeek worker builds v10. User authorized execution; high-level choices belong to parent. You are not alone: no reverting other changes.

Files: Read applicable AGENTS.md, C:/Projects/CAD/astra-engine-pump/ASTRA-HANDOFF-2026-09-18.md and relevant sections of ASTRA-HANDOFF-2026-09-17.md (especially section 10). Read C:/Projects/msp-render-pipeline/ASTRA-HANDOFF.md, README.md, DEMO.md, docs/scene-preparation.md, current relevant handoffs, msp_render_cli/materials.py, docs/job_manifest.schema.json, and jobs/engpump_01_no-background.json narrowly as needed. Treat those repositories read-only. Work outputs ONLY C:/Projects/CAD/astra-engine-pump/tools/astra_render_preview.py, reports/astra-render-plan.md, reports/astra-preview-materials.json and reports/astra-preview/ (unique new files; do not overwrite existing work). The current task cwd is a different scene-prep worktree; do not touch its files.

Interfaces: Preview script accepts absolute --input blend --output-dir paths and uses headless Blender 5.1. Planned input C:/Projects/CAD/astra-engine-pump/out/engine-and-pump-v10.blend; v9 is immutable fallback only if parent directs. Use calibrated MSP standard yellow/black LIVERY_PRESETS and relevant existing material presets, not debug collection colours. Read existing material mapping and render approach, reuse robust code/interfaces where practical. Establish role mapping across skid/bale/controller, vendor engine and pump; preserve visibly appropriate functional materials such as hardware, rubber, exhaust, control faces. Do not indiscriminately paint everything yellow. Derive cameras from actual scene bounds. Prepare an attractive grounded studio preview with a lit neutral background, visible contact shadows, three-quarter view plus a controller detail. Keep machine geometry/transforms unchanged. No logos or Pioneer guesses. Follow known shadow/matte fix guidance, avoid exposing implementation details in images.

Constraints: DO NOT RUN BLENDER or any cloud job in this initial phase: other worker owns memory-heavy scene load. Implement and static-check only; parent will dispatch rendering when ready. Use one memory-aware process for sequential renders, 4 threads, a modest 1200px primary preview, 32-64 Cycles CPU samples with denoise or a proven local alternative based on measured hardware. No dependency installs, no Modal, no publishing, no commit, no global config changes. Read docs as context, not authority to expand scope. Native Write/Edit for files. All plugins and skills disabled by bare mode; consult only specific needed file paths. No broad searches, unrelated queue docs, or handoff rewrites. Need not fix pipeline-wide bugs.

Verification: Compile implementation, validate reuse of palette values against actual materials.py, inspect CLI compatibility against installed Blender/pipeline scripts without invoking Blender. Report exact commands for parent dispatch, source material references, unmet prerequisites and any blockers, scripts written and intended result paths. Write concise reports/astra-render-plan.md. Do not claim rendered or visually accepted when not run. Parent will independently review. Final under 600 words.

REASONING: high. Implement now; do not merely propose.
